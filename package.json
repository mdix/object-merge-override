{
  "name": "object-merge-override",
  "version": "0.1.0",
  "description": "Merge object properties from multiple objects into one",
  "main": "object-merge-override.js",
  "repository": "https://github.com/mdix/object-merge-override",
  "scripts": {
    "test": "mocha test/"
  },
  "author": "Marc Dix",
  "license": "MIT",
  "dependencies": {
    "sort-object": "^0.3.1"
  },
  "devDependencies": {
    "mocha": "^2.0.0",
    "expect.js": "^0.3.1"
  }
}
