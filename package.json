{
  "name": "object-merge-override",
  "version": "0.1.0",
  "description": "Merge object properties from multiple objects into one",
  "main": "index.js",
  "scripts": {
    "test": "mocha test/"
  },
  "author": "Marc Dix",
  "license": "ISC",
  "dependencies": {
    "sort-object": "^0.3.1"
  },
  "devDependencies": {
    "mocha": "^2.0.0",
    "expect.js": "^0.3.1"
  }
}
